{
    "speed": [
      { "if": "true", "limit_to": 5.2 },
      { "if": "road_class == STEPS", "multiply_by": 0.7 }
    ],
    "priority": [
      
      { "if": "road_access == PRIVATE || road_access == NO", "multiply_by": 0 },
      { "if": "road_class == MOTORWAY || road_class == TRUNK", "multiply_by": 0 },
  
      { "if": "road_class == PRIMARY || road_class == SECONDARY", "multiply_by": 0.3 },
      { "if": "road_class == TERTIARY", "multiply_by": 0.6 },
  
      { "if": "road_class == SERVICE", "multiply_by": 0.7 },
  
      { "if": "road_class == RESIDENTIAL || road_class == LIVING_STREET || road_class == PEDESTRIAN", "multiply_by": 1.4 },
      { "if": "road_class == FOOTWAY || road_class == PATH || road_class == CYCLEWAY || road_class == STEPS", "multiply_by": 1.5 },
  
      { "if": "surface == ASPHALT", "multiply_by": 0.9 },
      { "if": "surface == CONCRETE", "multiply_by": 0.9 },
      { "if": "surface == PAVING_STONES || surface == FINE_GRAVEL", "multiply_by": 1.25 },
      { "if": "surface == GRAVEL || surface == DIRT || surface == GRASS", "multiply_by": 1.1 }
    ],
    "distance_influence": 220
  }